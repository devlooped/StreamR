namespace Devlooped.AI;

public static class ThreadManagerExtensions
{
    //static Dictionary<string, string> emptyMetadata = [];

    //public static Task<Thread> CreateAsync(this IThreadManager manager) =>
    //    manager.CreateAsync(emptyMetadata);

    //public static Task AppendAsync(this IThreadManager manager, string message) =>
    //    manager.AppendAsync(message, new UserContent(message));
}